{
  "name": "@myclaw/agent",
  "private": true,
  "scripts": {
    "dev": "PYTHONPATH=src .venv/bin/python -m uvicorn myclaw_agent.server:app --reload --port 8000",
    "build": "python3 -c \"print('agent build: n/a')\"",
    "lint": ".venv/bin/python -m ruff check .",
    "format": ".venv/bin/python -m ruff format .",
    "typecheck": ".venv/bin/python -c \"print('python typecheck: add mypy if you want strict typing')\""
  }
}
